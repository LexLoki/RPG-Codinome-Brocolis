require "bullet"

BulletSenoid = class_extends(bullet,"senoid")

function BulletSenoid.new(x,y,player)
  BulletSenoid.newObject(x,y,player)
  --other variables
end

function BulletSenoid.update(dt)
  
end